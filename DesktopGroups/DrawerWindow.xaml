<Window x:Class="DesktopGroups.DrawerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True" SizeToContent="Height"
        Width="230" ResizeMode="NoResize">
  <Window.Resources>
    <DropShadowEffect x:Key="Shadow" BlurRadius="20" ShadowDepth="0"
                      Color="#000000" Opacity="0.45"/>
    <Style x:Key="ShortcutBtn" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="Padding" Value="10,0"/>
      <Setter Property="Height" Value="36"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="6"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#2AFFFFFF"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>

  <Border x:Name="MainPanel" Background="#F21E1E2E" CornerRadius="12"
          Margin="12,4,12,12" Effect="{StaticResource Shadow}"
          AllowDrop="True" Drop="MainPanel_Drop" DragOver="MainPanel_DragOver">
    <StackPanel Margin="0,8,0,8">
      <DockPanel Margin="12,4,10,4" LastChildFill="True">
        <Image x:Name="ImgGroupIcon" Width="18" Height="18"
               DockPanel.Dock="Left" Margin="0,0,8,0" VerticalAlignment="Center"/>
        <TextBlock x:Name="TxtGroupName" Foreground="White" FontSize="13"
                   FontWeight="SemiBold" VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis"/>
      </DockPanel>
      <Rectangle Height="1" Fill="#33FFFFFF" Margin="10,2,10,4"/>
      <ItemsControl x:Name="ShortcutList" Margin="6,0">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Button Style="{StaticResource ShortcutBtn}"
                    Tag="{Binding TargetPath}"
                    Click="ShortcutBtn_Click" Margin="0,1">
              <Button.ContextMenu>
                <ContextMenu>
                  <MenuItem Header="Eliminar del grupo"
                            Tag="{Binding TargetPath}"
                            Click="RemoveShortcut_Click"/>
                </ContextMenu>
              </Button.ContextMenu>
              <StackPanel Orientation="Horizontal">
                <Image Source="{Binding Icon}" Width="16" Height="16"
                       Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Name}" Foreground="#E0E0E0" FontSize="12"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                           MaxWidth="155"/>
              </StackPanel>
            </Button>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </Border>
</Window>
