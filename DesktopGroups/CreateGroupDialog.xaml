<Window x:Class="DesktopGroups.CreateGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nuevo Grupo" Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#FF1E1E2E">
  <Window.Resources>
    <Style x:Key="Btn" TargetType="Button">
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="7"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <StackPanel Margin="26,22,26,22">
    <TextBlock Text="Nuevo Grupo" Foreground="White"
               FontSize="17" FontWeight="Bold" Margin="0,0,0,16"/>
    <TextBlock Text="Nombre" Foreground="#888888" FontSize="11" Margin="0,0,0,6"/>
    <Border Background="#FF313244" CornerRadius="7" Margin="0,0,0,22">
      <TextBox x:Name="TxtName" Text="Nuevo Grupo" Background="Transparent"
               Foreground="White" BorderThickness="0" Padding="12,9" FontSize="14"
               KeyDown="TxtName_KeyDown"/>
    </Border>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancelar" Click="Cancel_Click" Margin="0,0,8,0"
              Background="#FF313244" Foreground="#AAAAAA" Style="{StaticResource Btn}"/>
      <Button Content="Crear" Click="Create_Click"
              Background="#FF7C3AED" Foreground="White" Style="{StaticResource Btn}"/>
    </StackPanel>
  </StackPanel>
</Window>
